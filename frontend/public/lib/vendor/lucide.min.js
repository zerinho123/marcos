/*!
 * lucide-static - v1.25.0 - ISC license — https://lucide.dev
 *
 * Vendorizado em 2026-07-22 (D11 do SDD — sem CDN, CSP scriptSrc:['self']).
 * Subconjunto de ícones extraído de npm lucide-static@1.25.0 (icons/*.svg,
 * paths verbatim). Pra adicionar: copiar o <path> de icons/<nome>.svg pro ICONS.
 */

const ICONS = {
  'trending-up': '<path d="M16 7h6v6" /><path d="m22 7-8.5 8.5-5-5L2 17" />',
  'trending-down': '<path d="M16 17h6v-6" /><path d="m22 17-8.5-8.5-5 5L2 7" />',
  'alert-triangle': '<path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3" /><path d="M12 9v4" /><path d="M12 17h.01" />',
  x: '<path d="M18 6 6 18" /><path d="m6 6 12 12" />'
};

// Adapter: devolve o markup <svg> pronto. currentColor herda a cor do texto do
// elemento pai (igual aos glifos unicode que substitui).
export function icon(name, { size = 16, className = '' } = {}) {
  const paths = ICONS[name];
  if (!paths) return '';
  const cls = className ? ` class="${className}"` : '';
  return `<svg${cls} xmlns="http://www.w3.org/2000/svg" width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${paths}</svg>`;
}
