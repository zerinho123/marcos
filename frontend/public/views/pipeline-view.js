// ============================================================================
// views/pipeline-view.js — placeholder (pipeline vive no CRM)
// ============================================================================

import { renderStubView } from './shared.js?v=20260727-finance-v87';

export function renderPipelineView() {
  return renderStubView(
    'Pipeline CRM',
    'Pipeline comercial vive no módulo CRM. Acesse cfsistema.site/app pra gerenciar.'
  );
}
